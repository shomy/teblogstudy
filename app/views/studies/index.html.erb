<div class="container">
  <div class="wrapper col-md-8 col-md-offset-2 col-sm-10">
    <p><%= notice %></p>
    <h1>ブログ一覧 </h1>
    <% @studies.each do |study| %>
      <div class="row">
        <!-- # ブログのタイトルにlink_toでリンクを設定します。 -->
          <p><%== markdown(study.content) %></p><hr><hr>
      </div>
    <% end %>
  </div>
</div>

